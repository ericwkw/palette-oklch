# Palette — an OKLCH ramp studio

**[Open it →](https://ericwkw.github.io/palette-oklch/)** — published from `main`, and only when the checks pass.

A single HTML file. Open `index.html` in a browser; nothing to install. It is assembled from the parts in `build/` — edit those and run `./build.sh` (or `npm run build`). The committed page and its parts are checked against each other on every push, so the two cannot drift apart.

Colour is generated in OKLCH and stays there: every ramp sits on the same lightness steps, so a shade means the same weight whatever the hue, and chroma is fitted to the gamut step by step so no swatch is a lie.

## What it does

- **Light / dark switch** in the tab bar: Light, Dark or Both. Every view follows it — ramps, roles, tokens, contrast, opacity, gradients and preview are rebuilt for the chosen theme, and the tool's own page follows too. Both shows them one under the other.
- **One set of hues serves both themes.** Hue and colourfulness are shared; only the lightness curve differs, and tokens read the ramp from the opposite end (paper is step 50 in light, 950 in dark). Two dark-mode controls adjust that curve: how far the deep end lifts, and how colourful the dark theme runs.
- **Start from a hex.** Paste a brand colour into a role's *Brand hex* field: the ramp is built around it, and by default your exact value replaces the step it lands on, marked *brand* in the ramp. Untick *Keep my exact colour in the ramp* to see the generated step instead, with yours beside it for comparison. Move the hue or colourfulness away and the line says the ramp has drifted, with a button to snap back to your colour; ✕ forgets it. The exact value is always exported as `--main-brand` and friends.

- **Hue turns along a ramp.** A single hue reads wrong at the extremes — dark yellows go acid, pale reds go pink. Each ramp turns its hue between the light and dark ends in the direction that family wants (yellows toward orange as they darken, blues toward indigo), scaled by one control, or held flat at zero. Neutrals never turn, and a pinned brand colour is the pivot the turn happens around.
- **Where the steps sit is a choice, and it is shown.** Tailwind's own curve bunches the pale steps together and spreads the middle ones — 50 and 100 sit 2.8 points of lightness apart while 400 and 500 sit 9.6 apart, which is a real decision made on your behalf by every tool that ships it silently. The gap between each pair is printed under the ramps, the narrow ones are marked, and *Step spacing* offers **even lightness** (7.2 points throughout) or **even contrast** (every neighbouring pair at 1.30–1.32:1) instead.
- **Ramps** on the shadcn / Tailwind scale — 50, 100 … 900, 950 — for main, supporting, an accent pair, neutrals, and six functional colours. Light and dark come from one set of hues.
- **Roles and tokens.** Roles say what a colour is for; tokens are the shadcn names a product uses. Ramp steps underneath are the primitives, so a palette swap never renames a token.
- **Share of surface.** Set how much of a screen each role should hold. The five always add up to 100% — move one and the rest rebalance in proportion, so there is no arithmetic to do.
- **Contrast.** A token audit first: every pair the tokens actually create — foreground on background, muted-foreground on muted, border on card, ring and chart colours — checked at the level each needs (4.5 text, 3.0 marks, hairlines reported not failed). Then text on surfaces, and every solid fill with its own label, including which step to use when one falls short. Defaults pass in both themes.
- **Opacity and overlays.** Every role at 4–80% over each surface, with the exact solid it composites to and the ramp step it matches, so a tint can be swapped for a token. Drop in an image and a caption box appears over it: drag or resize the box to where the caption will really sit, and the pixels inside it are measured by percentile — the median, the 95th and the single brightest, each with the black scrim white text would need over it. The recommendation follows the 95th, so one glint of sunlight does not dictate a heavy scrim for the whole picture, and the box wears the scrim it recommends so you read the real thing.
- **Gradients** interpolated in OKLCH, with a banding note and a sentence saying how far along the band white text survives — *holds for the first 50% of the band, then fails* — measured across twenty-one samples rather than at the two ends.
- **A preview worth trusting.** One dense screen rather than a few swatches: a sidebar with an active item, a table of six rows carrying all six functional statuses, a form with a focused field, an error and a disabled field, a five-series chart on the chart tokens, and body text at three sizes. Drawn only from tokens, in light and dark, so a mapping change has somewhere to show.
- **A library of palettes.** Keep the current palette under its name, switch between kept ones, duplicate, and delete. Kept in this browser, and the line under the list says whether what is on screen still matches what you kept. Renaming and keeping makes a new palette rather than overwriting the old one; deleting asks once and removes only the saved copy, never the colours on screen.
- **Undo and redo** over the last 30 changes, with ⌘Z and ⇧⌘Z. A whole slider drag counts as one change, a new change after undoing drops the branch ahead of it, and switching library palettes can be undone like anything else.
- **The page opens on its subject.** The ways in — six to look at, from a picture — are one line of buttons above the ramps, each opening in place when it is asked for and folded the rest of the time. The shelf sits behind one button in the rail, with its count on it, reachable from every tab. Nothing empty talks at you.
- **Start from a picture.** One picture serves both jobs: the colours it is made of, and the scrim a caption needs over it — dropped in either place, loaded for both. Its pixels are read in OKLCH, gathered into the few colours the picture is actually made of — with the share of the image each one covers, and its greys offered separately as a ground — and any of them can become Main, Supporting or either accent, be handed to a sister brand, or seed six palettes around itself. Clicking the picture is an eyedropper for the exact pixel.
- **A shelf for what you have not decided about.** The library is for palettes you have named and kept; the shelf is for the ones you were only trying. Anything that replaces your palette — a suggestion taken, a colour from a picture, a reset, a shuffle — leaves the old one on the shelf first, and you can put one there yourself. Click a chip to have it back, and the one it replaced is caught in turn. Kept in this browser, up to twenty-four deep, nothing needs naming — and nothing is dropped or cleared without asking first, since the shelf is holding the versions nothing else is.
- **Looking costs nothing.** *Show me six* puts six whole palettes on screen — each one drawn through the real ramp engine, each clear of the state colours — and changes nothing until you click one. Six more, or put them away. Taking one is a single undoable change. The family has its own version: *Suggest three* offers three siblings placed clear of the parent and of every state colour.
- **One brand's screen at a time.** The family previews used to draw every brand at once, which made six sisters ten screens tall. The wheel is the summary; below it, one brand's screen is drawn, chosen from a row of chips or by grabbing its dot. Six sisters is now five screens rather than ten and a half.
- **The family on a wheel.** Parent and sisters as dots on one circle: hue round, colourfulness in and out, the arcs the state colours have spoken for shaded outside the ring, a ringed dot for a brand colour you pasted. Drag a dot to move that brand, or focus it and use the arrow keys — round for hue, in and out for colourfulness, with Shift for bigger steps. Either way it is the same change as moving that brand's two sliders, named in the history the same way. One picture instead of a column of cards.
- **Families have shapes.** Spread, analogous (a fan to one side), two wings (warmer and cooler in pairs), or triad, with a width you can open and close. Nothing is placed within 26° of the parent or on a state colour, and when the siblings will not fit the width you asked for, the tool widens it and says so.
- **A brand family.** One parent and as many sisters as you need. A sister has its own brand hex, a line saying what it is for, and its own main hue and colourfulness — it may travel a long way round the wheel — and inherits the parent's neutrals, the six functional colours, the lightness steps, the ramp shape and the token mapping, which is what keeps them reading as one family. Sisters closer than 24° to each other, or sitting on a functional hue, are called out; *Space them evenly* pushes them apart and steps off the reserved hues. A parity table asks whether every brand's button reads as strongly as the parent's, the preview is drawn once per brand side by side, and the family exports as one stylesheet with `:root` for the parent and `[data-brand="…"]` for each sister — keyed on a slug fixed when the sister is created, so renaming it on screen never breaks a stylesheet already in use.
- **Export, in the shapes the next person needs.**
  - **A link** that carries the whole palette in its own hash — nothing is uploaded and nothing is stored on a server. Opening it brings the tool up exactly as it was, then clears the hash so a later reload shows your own work.
  - **shadcn `globals.css`** — `:root` and `.dark`, all thirty token names, in OKLCH, plus the raw ramps as custom properties. The token names were checked against a published shadcn theme.
  - **Tailwind**, either version: v4 as an `@theme inline` block whose colours follow `.dark` on their own, or v3 as a `tailwind.config.js` whose semantic colours point at the CSS variables and whose ramps are literal.
  - **Figma variables** — one collection with a Light and a Dark mode, so a token switches mode in Figma the way it switches class in the browser. Semantic tokens, every ramp step, and any pasted brand colour. In Display P3 the file says so, marks every value that lies outside sRGB, carries its wide value beside the hex as `oklch`, and counts them in a note, rather than clipping quietly.
  - **A PNG sheet** of every ramp with its steps and hex values, and a token strip for each theme.
  - **JSON** of the ramps and settings. Save and load a palette file.
- **Two tabs, two palettes.** The palette you are working on lives in this tab alone, so a second tab cannot write over it — which is what used to happen, silently, to whichever tab reloaded second. A new tab starts from the last palette left in this browser, the library is deliberately shared between tabs and kept in step, and a tab says when another one is at work.
- **A saved palette can never blank the tool.** Everything read from storage, from a file or from a link is checked value by value: anything unusable is replaced with its default, anything unknown is ignored, and the tool says how many values it had to repair. If a render throws even so, a line at the top says what went wrong and offers a fresh start, instead of a page with no colour on it.

- **The strongest step is the one reported.** A screen cannot hold much chroma at the pale or the deep end, so asking for the peak near either extreme pulls it back toward the middle. The number beside the slider is the step that really ends up strongest, and a line says what was asked for and what the gamut allowed.
- **Nudge one step.** Click any swatch to open an editor and move that step's lightness or colourfulness on its own; nudged steps carry a dashed outline, and a reset puts them back. A nudge moves one step but changes two gaps, so the editor reports both — the distance and the contrast to each neighbour — and says when the step has come close enough to one of them to look like the same colour, or left a hole much wider than the rest of the ramp.
- **Share of surface, measured.** The five sliders say what each role *should* cover. The tool then reads the preview it just drew — element by element, by area, attributing each background to the ramp it came from — and reports what that screen actually covers against what you asked for. One screen is not a whole product, so it is offered as a sanity check on the intention rather than a verdict; it is, at least, a measurement rather than a restatement of the sliders.
- **Choose what each token uses.** Primary, tint surface, hairline, focus ring and the chart series can each point at a different step, per theme. A step your pasted brand colour landed on is offered by name, even when it falls outside a control's usual range.
- **Does the brand colour actually reach the interface?** Pinning a hex into a ramp is not the same as a token using it: a colour pinned at step 400 while every token reads 100 and 700 appears nowhere in the product. The Roles tab names, for each pinned colour, the tokens that draw it — or says plainly that none do, and offers the control that would mend it. The rail carries the short version beside the colour you pasted.
- **A failing contrast pair says what would fix it.** The summary names the pairs that fall short rather than counting them, the rows are marked, and each carries the step that clears the level — one click, taken on the control that owns it.
- **Gamut, honestly.** In P3 the swatches are painted in OKLCH and a ▲ marks the values a plain sRGB screen cannot reach; the hex shown stays the closest sRGB fallback.

- **Quiet by default.** The lines that explain what a control is for are off until asked for: **Explain** in the rail turns every one of them on, and the choice is remembered. The tool goes from 911 words of explaining on screen to about a dozen — and what it has to *say about your palette* is not explaining, so the audit verdicts, the clash warnings, where your brand colour landed and how many versions are on the shelf are never hidden.
- **Plain words, with the jargon a click away.** The controls are named for what they do — *Colourfulness*, *Strongest at*, *Dark fade*, *Hue turn*, *Screen range* — and each carries a question mark that opens a glossary at that entry. Twenty-two entries, and a journey fails if the tool grows an idea the glossary has not caught up with.
- **Only what you are looking at is drawn.** The rail and the ramps are always current; the other views are marked stale and built when you open them. With a family of ten brands, six slider steps take about 13ms from the ramps tab instead of about 94ms.

- **A rail you can get around.** Eleven sections in one column was a scroll-and-hunt, so each folds to a line that still says what is inside it — the hue and a swatch, how many palettes are kept, how many changes there are to undo — and what you leave open is remembered.
- **Undo says what it would undo.** *Undo hue on main*, *Undo adding a sister*, *Undo pasting #0075C9* — the label comes from the control or the action, so pressing it is not a guess.
- **Colour first in the ramps.** Step numbers and hex values fade in on hover, or all at once behind a switch, so eleven rows read as colour rather than as a wall of text. Pinned and nudged steps keep their labels, because those need to be visible without hunting.
- **An order to the work.** A strip above the tabs: give it your colours, check they are used, clear the audit, keep the states distinct, hand it over. Each says where it stands — *2 pinned*, *1 unused*, *all clear*, *2 too close* — and takes you to the tab that does it.

- **Colour vision.** The status colours differ by hue, and hue is exactly what a colour-blind eye loses. The Contrast tab puts your own colours through the cone response of deuteranopia, protanopia and tritanopia (Viénot, Brettel & Mollon, in linear light), lists the pairs that come together — on the defaults, Success and Danger fall from 0.276 apart to 0.014 under deuteranopia — and says to give those a shape, an icon or a word as well as a colour. The preview can be viewed through the same eye.
- **Held to its own standard.** The tool's own interface is measured by the same `wcag()` it measures your palette with: every piece of text in the rail and the views clears 4.5:1 in both themes, and the red it warns you with is readable rather than merely red. A journey walks the live interface and fails if any of it falls short.
- **Keyboard and screen reader.** Swatches are buttons: focusable, pressable with Enter or Space, each announcing its role, step and value. Copying and opening the step editor are announced through a live region, the tabs are a real tablist, and there is a focus style of the tool's own.

## Checking it

`journeys.mjs` drives the tool with real clicks in headless Chrome and checks sequences, not states — what happens *next* after a change, whether it can be seen, and whether it can be undone.

```bash
npm test
```

That builds the page from its parts, fails if the committed `index.html` is not what those parts make, and drives the tool through every journey in headless Chrome. It needs Node 22 or newer (for its built-in WebSocket) and a Chrome; it starts its own server and installs nothing. The pieces run on their own too — `npm run check`, `npm run journeys`, `npm start` to just serve it — and `node journeys.mjs http://host/page` points them at a deployed copy.

The same three steps run on every push and pull request, and `main` publishes to GitHub Pages only if they pass.

Forty-five journeys: a brand colour survives being worked around and can be recovered; the pin toggles with a real click; every token mapping moves something on screen; a step can be nudged and reset; the theme switch reaches every tab; work survives a reload; the defaults pass their own contrast audit; the hue turns along a ramp without leaving its family; the preview uses every token and reacts to a mapping; the library keeps, switches, duplicates and deletes without losing work; undo and redo walk the history, by button and by keyboard; a sister joins the family, shares the right things and is warned when it collides; the family holds its contrast parity; the scrim check follows the caption box across a generated test image and reports by percentile rather than by one bright pixel; every export parses — the browser is made to read back each colour, the v3 config is actually run, the Figma JSON is parsed — and the link round-trips through a real page load; every question mark in the rail has a glossary entry behind it and no control is labelled with jargon; with ten brands a drag stays fast and the view left behind is brought up to date when it comes back; a brand colour no token uses is reported as unused and the offered fix really mends it; a failing contrast pair names itself and is cleared from where it is reported; the share of surface is measured off the drawn preview and disagrees with the target when the target is wrong; the state colours can be moved, warn when they read as brand, and are put back to their defaults; the tool can be got around — the rail folds and remembers, undo names the change, the ramps lead with colour, and the steps reflect the state and go where they say; the tool's own interface clears 4.5:1 in both themes; an untouched palette passes every check it ships with; the pairs that collapse for a colour-blind eye are found and the preview can be seen through that eye; the ramps can be used without a mouse; and what the tool says about itself is true — the gradient notes are sentences that agree with their colours, the P3 export admits what it cannot carry, the tab bar holds one line at 1280 and 1440; two tabs keep their own work and say so; a broken, nonsensical or newer-than-this saved palette still draws and reports what it repaired; the peak control reports the step it delivers rather than the one it was asked for; the spacing of the steps is shown, can be changed to even lightness or even contrast, and the choice reaches the export; the step editor reports the gaps a nudge creates; six candidates can be looked at without changing anything and taking one is undoable; and a family can be explored — siblings suggested clear of everything, dots dragged on the wheel, four shapes that keep their distance, a sister carrying its own hex, purpose and stable selector; a picture gives up the colours it is made of and seeds palettes near them; the shelf catches whatever gets replaced, gives it back, and survives a reload; the ramps start in the top half of the page with nothing empty lecturing above them and only one way in open at a time; a picture loaded once reaches the scrim check and the sisters as well; a family of six stays under six screens with one brand drawn at a time; the shelf asks before it destroys anything; the wheel answers the arrow keys; every idea the tool uses has a glossary entry behind it; a panel left open comes back with its contents; every step in the checklist can actually be reached; and the tool is quiet until asked to explain itself, without ever hiding what it has to say about your palette. Exit code 1 if any fail.

Each of those exists because a spot check missed it.

**The journey comes first.** Write it, run it, and watch it fail for the reason you expect before writing the code that makes it pass — a test that has never been red has not been shown to test anything. If the new one turns an old one red, that is the old behaviour being deliberately replaced, and it is worth saying so out loud rather than editing quietly.

Every new control should answer three questions: what does it change on screen, how is it undone, and what does it look like in a strange state. Not everything is test-shaped — proportion, density and whether a screen reads well are still judged by looking — but once such a thing has a number, it becomes an assertion: page height, contrast ratio and element counts all arrived here that way.

## Functional colours

Success, warning, danger, info, pending and new. They start on fixed hues, because in a brand family meaning should not move when a sister changes its main colour — but they can be moved, each with its own hue and colourfulness, and put back to the default one at a time or all at once. Whatever they are set to is what the family shares.

The check that matters is whether a state can still be read *as a state*: any that sits within 25° of one of your brand colours, or within 18° of another state, is named. *Move them off the brand hues* walks each one outward until it is clear, but never further than 45° from where it started — a Danger that lands in the greens has stopped meaning danger — and anything it cannot free is reported rather than quietly left.

## Planned

- Palette comparison side by side, and colour-blind simulation.
